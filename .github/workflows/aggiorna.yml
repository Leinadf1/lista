name: Auto Update
on:
  schedule:
    - cron: '0 * * * *' # Questo lo lasciamo per sicurezza
  workflow_dispatch:      # QUESTA RIGA È FONDAMENTALE per far funzionare la sveglia
  push:
    branches: [ main ]
